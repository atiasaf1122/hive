"""Lessons store — the learning loop (Phase D1).

Episodic events → durable procedural lessons. The write path is
GROUNDED-ONLY (distilled from objective evidence, never a model's
self-diagnosis), gated by a trustworthiness check, retrieved
conservatively (zero injections is a normal outcome), and hygiened by
outcome (confirm / archive).
"""
